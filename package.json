{
  "name": "runloop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "cap:prepare": "node scripts/prepare-capacitor-web.mjs",
    "cap:add:ios": "npm run cap:prepare && npx cap add ios",
    "cap:sync:ios": "npm run cap:prepare && npx cap sync ios",
    "cap:sync:ios:web": "npm run cap:sync:ios && node scripts/use-ios-remote-url.mjs",
    "cap:open:ios": "npx cap open ios",
    "cap:run:ios": "npm run cap:sync:ios && npx cap run ios",
    "cap:run:ios:web": "npm run cap:sync:ios:web && npx cap run ios --no-sync"
  },
  "dependencies": {
    "@capacitor/core": "8.3.4",
    "@capacitor/geolocation": "8.2.0",
    "@capacitor/splash-screen": "8.0.1",
    "@capacitor/status-bar": "8.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.3.4",
    "@capacitor/ios": "8.3.4"
  }
}
